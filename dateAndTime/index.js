var format = require('date-format');

// Create a new Date object
var date = new Date();

// Format the date in the desired format, for example 'yyyy' for the year
console.log(format('yyyy'-'dd'-'MM', date));
